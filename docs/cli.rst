.. _cli:

Temple CLI
==========

The main ``temple`` command and its subcommands are listed below. Note that
``--help`` can be given as an argument to any of these commands to print
out help on the command line.

.. click:: temple.cli:main
    :prog: temple
    :show-nested:
